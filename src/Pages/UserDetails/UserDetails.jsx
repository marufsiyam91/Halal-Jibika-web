import { useLoaderData } from "react-router-dom"

const UserDetails = () => {
  const details = useLoaderData();
  return (
    <div>
          <h1>User Details</h1>
           
    </div>
  )
}

export default UserDetails
